create table person(id identity primary key, 
fname varchar(50), lname varchar(50), dob date);


create table PostContent(id identity primary key,
title varchar(50),description varchar(1500),category varchar(50),
mydate date);

